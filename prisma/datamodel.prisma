type Post {
  id: ID! @id
  createdAt: DateTime! @createdAt
  updatedAt: DateTime! @updatedAt
  published: Boolean! @default(value: false)
  title: String!
  content: String!
  author: User!
}

type User {
  id: ID! @id
  email: String! @unique
  password: String!
  posts: [Post!]!
  expenses: [Expense!]!
}

type Expense {
  id: ID! @id
  type: String!
  description: String!
  date: String!
  amount: Float!
  createdBy: User!
}